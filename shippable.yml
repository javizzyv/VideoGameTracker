# Indico que voy a usar Nodejs
language: node_js
# Indico las versiones que quiero que se prueben
node_js:
    - 12.18.4
    - 12
    - 11
    - 10
# Instalo dependencias y hago los test
build:
    ci:
        - npm install --global gulp-cli
        - gulp install
        - gulp test